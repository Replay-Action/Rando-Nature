{{ form_start(form) }}
<div class="row col-lg-12 justify-content-center">
    <div class="abcdef col-lg-12">
            <p>Nouveau Référent :</p>
            <p>{{ form_widget(form.nom) }}</p>
    </div>

    <div class="col-lg-12 button-enregistrer-new-referent">
        <div>
        <button class="mb-5 mt-2 btn btn-primary btn btn-lg">{{ button_label|default('Enregistrer') }}</button>
        </div>
        <div>
        <a href="{{ path('referent') }}" class="mb-5 mt-2 btn btn-primary btn btn-lg">Retour</a>
        </div>
    </div>

</div>
{{ form_end(form) }}